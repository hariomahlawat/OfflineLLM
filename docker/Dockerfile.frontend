# Dockerfile for building and serving the Offline LLM UI with Nginx
# This Dockerfile builds the UI using Node.js and serves it with Nginx over HTTPS


FROM node:20 AS build

WORKDIR /ui

COPY offline-llm-ui/package.json offline-llm-ui/package-lock.json ./
RUN npm install

COPY offline-llm-ui/ ./
RUN npm run build

# Serve with nginx + HTTPS
FROM nginx:alpine
COPY docker/nginx.conf /etc/nginx/nginx.conf
COPY --from=build /ui/dist /usr/share/nginx/html
COPY certs /etc/nginx/certs
EXPOSE 443

CMD ["nginx", "-g", "daemon off;"]
