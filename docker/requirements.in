# ASGI / API stack
fastapi==0.115.5
uvicorn[standard]==0.30.6

# LangChain ecosystem (0.1.x line)
langchain==0.1.17
langchain-community==0.0.38
langchain-chroma==0.1.4        # downgraded to stay compatible with langchain 0.1.x
chromadb==0.4.24

# Vector / model inference
sentence-transformers[onnx]==2.7.0
onnxruntime==1.18.1

# Torch stack (CPU only). Remove torchvision / torchaudio if unused.
torch==2.4.1
# torchvision==0.19.1
# torchaudio==2.4.1

# Data validation
pydantic==2.9.2

# Document processing
pymupdf==1.24.10
pypdf==5.0.1

# Ollama interface
ollama==0.3.3
langchain-ollama==0.0.7

# File uploads
python-multipart==0.0.9

# Build tool (only keep if actually needed at runtime)
# build==1.2.2.post1
